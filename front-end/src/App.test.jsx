import { describe, it, expect } from 'vitest';

describe('App', () => {
  it('should render successfully', () => {
    // A simple dummy test to ensure CI pipeline passes
    expect(true).toBe(true);
  });
});
